# Base URL of the Orderlynk backend API.
# Leave unset in development — Vite proxies /api to http://localhost:8080 (see vite.config.ts).
# Set this for production builds, e.g. https://api.orderlynk.app
VITE_API_URL=
